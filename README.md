# BKT Web

(WiP 👷) React app using Sanity.io

👉 bonniekthompson.com

# dev setup (macOS)

1. install homebrew
2. install yarn and nvm via homebrew
3. setup PATH for nvm
4. in /app and /sanity `nvm use && yarn && yarn dev`
