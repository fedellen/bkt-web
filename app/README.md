# bkt Web App

This is the React app for bonniekthompson.com. It is a work in progress and is built using [Sanity.io](https://www.sanity.io/).

# dev setup (macOS)

1. install homebrew
2. install yarn and nvm via homebrew
3. setup PATH for nvm
4. `nvm use && yarn && yarn dev`
